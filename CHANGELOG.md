# 变更日志

本文档记录 DBMeta 的主要版本变更。

格式参考 Keep a Changelog，版本号遵循 SemVer。

## [1.0.0-beta.1] - 2026-04-28

### Added

- 新增达梦数据库（`达梦数据库`）数据源支持，打通连接检查、查询执行、数据库/表列表与任务连接链路。
- 新增达梦连接库 `src/libary/dm/dm.go`，并引入 `gitee.com/chunanyong/dm` 驱动依赖。
- 新增数据源类型 Logo 能力：`datasource_type` 增加 `logo` 字段，并提供内置数据库类型默认 Logo 资源。
- 新增数据源类型 Logo 代理接口 `GET /v1/datasource_type/logo`，统一前后端环境下的本地图片访问方式。
- 新增任务运行状态字段 `task_option.last_run_status`，用于记录计划任务最近一次运行状态。

### Changed

- 版本标识由 `1.0.0-rc.1` 调整为 `1.0.0-beta.1`，用于当前阶段发布管理。
- `/setting/datasource` 页面增强：新增“数据源类型”管理弹窗，支持启用状态与排序的行内修改，并展示类型 Logo。
- `/task` 任务列表增强：新增“运行状态”展示（微章样式），并补充上次/下次运行时间展示格式。
- 计划任务默认策略调整：AI 注释生成、应用、准确度评估任务改为每日凌晨按顺序错峰执行。
- PostgreSQL 连接串构造优化为 URL DSN 并统一 `sslmode=disable`，增强特殊字符与无库名场景兼容性。
- 元数据与容量采集链路增强达梦适配，补充达梦 SQL 与结果字段兼容处理（含字段名大小写标准化处理）。

### Fixed

- 修复达梦数据源误走 Oracle 驱动导致的连接异常问题（如 `ORA-12537`）。
- 修复达梦查询页面数据库/表列表加载、表点击默认 SQL 生成与执行计划提示兼容问题。
- 修复数据源类型 Logo 在开发环境无法正确显示的问题（静态资源路径与访问方式不一致）。
- 修复 `check_datasource` 写入事件失败问题（补齐事件主键与必要字段，避免插入冲突）。
- 修复达梦元数据采集中的字段名大小写与歧义列问题，确保库/表/字段采集稳定性。

## [1.0.0-rc.1] - 2026-04-24

### Added

- 发布 DBMeta 首个公开候选版本（RC）。
- 提供数据库治理核心能力：元数据治理、数据质量治理、任务编排与 AI 辅助分析。
- 提供前后端一体化工程与 Docker 部署路径。

### Changed

- 持续优化系统结构、权限控制与查询能力。
- 完善前端交互与部分模块的可用性表现。

### Fixed

- 修复若干日志、权限与流程中的稳定性问题。

---

> 说明：历史早期提交信息粒度有限，以上版本内容按模块进行归纳。后续版本建议在每次发布时同步更新本文件。

